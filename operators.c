#include <stdio.h>
int main()
{
    int num = 15;
    if (num % 2 == 0)
    {
        printf("Even Number= %d\n", num);
    }
    else 
    {
        printf("Odd Number= %d\n", num);
    }
    return 0;
}